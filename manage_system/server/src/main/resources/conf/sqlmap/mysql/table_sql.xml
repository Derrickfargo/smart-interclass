<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<typeAlias alias="Table" type="com.incito.interclass.entity.Table" />

	<select id="getTableList" resultClass="Table">
		SELECT	
			id,room_id, number,ctime
		FROM 
			tb_table
		ORDER BY
			id
	</select>

	<insert id="saveTable" parameterClass="Table">
		INSERT INTO 
			tb_table(room_id,number,ctime)
		VALUES
			(#roomId#,#number#,now())
		<selectKey resultClass="int" keyProperty="id" > 
            SELECT @@IDENTITY AS ID 
        </selectKey> 
	</insert>
	
	<delete id="deleteTable" parameterClass="java.lang.Integer">
		DELETE FROM tb_table WHERE id = #id# 
	</delete>
</sqlMap>