<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<typeAlias alias="Device" type="com.incito.interclass.entity.Device" />

	<select id="getDeviceList" resultClass="Device">
		SELECT	
			tb_device.id,imei,table_id,tb_device.ctime,tb_school.name AS schoolName,
			tb_room.name AS roomName,tb_table.number AS tableNumber
		FROM 
			tb_device
		INNER JOIN tb_table ON tb_table.id = tb_device.table_id
		INNER JOIN tb_room ON tb_room.id = tb_table.room_id
		INNER JOIN tb_school ON tb_school.id = tb_room.school_id
		ORDER BY
			id
	</select>

	<insert id="saveDevice" parameterClass="Device">
		INSERT INTO 
			tb_device(imei,table_id,ctime)
		VALUES
			(#imei#,#tableId#,now())
		<selectKey resultClass="int" keyProperty="id" > 
            SELECT @@IDENTITY AS ID 
        </selectKey> 
	</insert>
	
	<delete id="deleteDevice" parameterClass="java.lang.Integer">
		DELETE FROM tb_device WHERE id = #id# 
	</delete>
</sqlMap>