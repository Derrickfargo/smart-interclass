<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<typeAlias alias="Classes" type="com.incito.interclass.entity.Classes" />

	<select id="getClassList" resultClass="Classes">
		SELECT	
			id,name,year,school_id AS schoolId,teacher_id AS teacherId,ctime
		FROM 
			tb_class
		ORDER BY
			id
	</select>

	<select id="getClassListBySchoolId" resultClass="Classes" parameterClass="java.lang.Integer">
		SELECT	
			id,name,year,school_id AS schoolId,teacher_id AS teacherId,ctime
		FROM 
			tb_class
		WHERE
			tb_class.school_id=#school_id#
		ORDER BY
			id
	</select>
	
	<insert id="saveClass" parameterClass="Classes">
		INSERT INTO 
			tb_class(name,year,school_id,teacher_id,ctime)
		VALUES
			(#name#,#year#,#schoolId#,#teacherId#,now())
		<selectKey resultClass="int" keyProperty="id" > 
            SELECT @@IDENTITY AS ID 
        </selectKey> 
	</insert>
	
	<delete id="deleteClass" parameterClass="java.lang.Integer">
		DELETE FROM tb_class WHERE id = #id# 
	</delete>
</sqlMap>