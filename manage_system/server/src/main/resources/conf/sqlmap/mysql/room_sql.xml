<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap>
	<typeAlias alias="Room" type="com.incito.interclass.entity.Room" />

	<select id="getRoomList" resultClass="Room">
		SELECT	
			tb_room.id,tb_room.name,school_id AS schoolId, 
			tb_school.name AS schoolName,mac,tb_room.ctime
		FROM 
			tb_room
		LEFT JOIN
			tb_school ON tb_school.id = tb_room.school_id
		ORDER BY
			id
	</select>

	<insert id="saveRoom" parameterClass="Room">
		INSERT INTO 
			tb_room(name,school_id,mac,ctime)
		VALUES
			(#name#,#schoolId#,#mac#,now())
		<selectKey resultClass="int" keyProperty="id" > 
            SELECT @@IDENTITY AS ID 
        </selectKey> 
	</insert>
	
	<delete id="deleteRoom" parameterClass="java.lang.Integer">
		DELETE FROM tb_room WHERE id = #id# 
	</delete>
</sqlMap>